import 'package:flutter/material.dart';

class AuditoriaDetailsScreen extends StatelessWidget {
  const AuditoriaDetailsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
